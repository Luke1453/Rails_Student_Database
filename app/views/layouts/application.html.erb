<!DOCTYPE html>
<html>

<head>
    <title>StaticAiBlog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
    <link rel="stylesheet" href="https://www.w3schools.com/lib/w3-theme-indigo.css">
</head>

<style>
    body,
    h1,
    h2,
    h3,
    h4,
    h5 {
        font-family: "Poppins", sans-serif
    }

    body {
        font-size: 16px;
    }
</style>

<body>



    <div class="w3-top w3-container w3-black" style="height:75px;width:100%;font-weight:bold;">
        <h1 class="w3-left">Rails Student Database</h1>

        <a href="https://github.com/Luke1453" class="w3-button w3-white w3-border w3-right w3-margin-top">Creator</a>
        <% unless current_user.nil? %>
        <%= link_to destroy_user_session_path, class:"w3-button w3-white w3-border w3-right w3-margin-top w3-margin", method: :delete do %>Log out<% end %>
        <% end %>

    </div>

    <% unless flash.empty? %>
    <script type="text/javascript">
        < % flash.each do |f | % >
                <
                %
                type = f[0].to_s.gsub('alert', 'error').gsub('notice', 'info') % >
                toastr['< %=type %>']('< %=f[1] %>'); <
            %
            end % >
    </script>
    <% end %>

    <div class="w3-container">
        <%= yield %>
    </div>
</body>

</html>